
include $(HALLD_HOME)/src/BMS/Makefile.JANA

include $(HALLD_HOME)/src/BMS/Makefile.SIMD

ADDITIONAL_MODULES += DANA PID TAGGER TRACKING START_COUNTER CDC
ADDITIONAL_MODULES += FDC TOF BCAL FCAL HDGEOMETRY HDDM JANA
