DIRS += include BCAL CDC DANA FCAL CCAL PID FDC START_COUNTER
DIRS += HDGEOMETRY TAGGER TOF TRACKING HDDM

ifdef AMPTOOLS
ifdef CLHEP

DIRS += AMPTOOLS_MCGEN AMPTOOLS_DATAIO AMPTOOLS_AMPS

endif
endif


include $(HALLD_HOME)/src/BMS/Makefile.dirs
