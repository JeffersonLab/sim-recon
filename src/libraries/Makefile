DIRS += include BCAL CDC DANA FCAL CCAL PID ANALYSIS FDC START_COUNTER CERE RICH
DIRS += HDGEOMETRY TAGGER TOF TRACKING TRIGGER HDDM

ifdef AMPTOOLS
ifdef CLHEP

DIRS += AMPTOOLS_MCGEN AMPTOOLS_DATAIO AMPTOOLS_AMPS

endif
endif


include $(HALLD_HOME)/src/BMS/Makefile.dirs
