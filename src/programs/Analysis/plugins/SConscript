

Import('*')

subdirs = ['acceptance_hists', 'b1pi_hists', 'cdc_hists', 'fcal_hists', 'phys_tree', 'mcthrown_hists', 'trackeff_hists', 'mc_tree', 'monitoring_hists']
SConscript(dirs=subdirs, exports='env osname installdir', duplicate=0)
