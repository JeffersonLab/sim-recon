
import sbms

Import('*')

# The following was never integrated into SBMS. This files is here mainly
# as a place holder. It was placed here when src/programs/Simulation/plugins
# was moved to src/plugins/Simulation just before the Git transition.

# Optional targets
sbms.OptionallyBuild(env, ['extract_ptype_hddm', 'hdparsim'])
